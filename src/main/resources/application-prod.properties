# Server Configuration
server.port=${PORT:8080}

# Database Configuration - SIMPLE AND DIRECT
# Use EXACTLY the environment variable names you set in Render
spring.datasource.url=jbc:postgresql://${springboot_db_jckr_host}:${springboot_db_jckr_port}/${springboot_db_jckr_db}
spring.datasource.username=${springboot_db_jckr_user}
spring.datasource.password=${qk470rwkI2IaKv1hU9Ob54xupD2gJ2N6}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration - Let Hibernate auto-detect the dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.open-in-view=false

# Connection pool settings
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5